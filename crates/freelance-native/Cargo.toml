[package]
name = "freelance-native"
version = "0.1.0"
edition = "2021"

[dependencies]
db-core = { path = "../db-core" }
rusqlite = { version = "0.32", features = ["bundled"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tauri = { version = "2", features = [] }
